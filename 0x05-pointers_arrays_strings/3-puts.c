#include "main.h"

/**
* _puts - prints a string to stdout
* str refers to selected string
* Return: Nothing
*/

void _puts(char *str);
{
char *str;

str = "Fear is the enemy of all dreams";
_puts(str);

Return (0);
}
